import { RoutePage } from "./route-page.js";
export default function Settings() {
  return (
    <RoutePage
      title="Settings"
      description="Configure connection, license, updates, CLI, and local logs."
    />
  );
}
